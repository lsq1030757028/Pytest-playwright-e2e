# Stage 3 测试设计

单元覆盖来源不可变、版本父链、变更权限、保障等级 Floor、非法 Campaign 转换、局部失效和有效进度回算。

阶段集成固定六个 TodoMVC 场景，并验证：普通澄清不重跑、Oracle 变化只使相关证据失效、跨域范围扩展升级到 L2、未授权建议不改变 Oracle、生产不变量冲突进入 BLOCKED。

资产：`tests/assets/harness/stage-3/governance-scenarios.yaml`。
