# Pinned target: percy/example-todomvc

This target is materialized from `https://github.com/percy/example-todomvc.git` at the exact commit recorded in `target.yaml`.

The application code is not copied into this repository. `TargetManager` clones the pinned revision, verifies `HEAD`, checks required files, installs dependencies without lifecycle scripts, and starts the application without a shell.

The upstream project is licensed under MIT and states that it was modified from TodoMVC.
